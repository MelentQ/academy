<div class="header-menu">
  <div class="header-menu__wrapper js-toggle-menu">
    <nav class="header-menu__list">
      {{#each menu}}

      <div class="header-menu__item js-root-menu-item">
        <a class="header-menu__item-head" href="#">
          <div class="header-menu__item-head-left">
            <span>{{this.name}}</span>
            {{#if this.tags}}
            <ul class="header-menu__item-head-tags">
              {{#each this.tags}}
              <li class="header-menu__item-head-tag" style="background-color: {{this.bg}}; color: {{this.color}};">
                {{this.text}}
              </li>
              {{/each}}
            </ul>
            {{/if}}
          </div>
          {{#if this.items}}
          <svg>
            <use xlink:href="#chevron">
          </svg>
          {{/if}}
        </a>
        {{#if this.items}}
        <div class="header-menu__item-body --level-2">
          {{#each this.items}}

          <div class="header-menu__item">
            <a class="header-menu__item-head" href="#">
              <div class="header-menu__item-head-left">
                <span>{{this.name}}</span>
                {{#if this.tags}}
                <ul class="header-menu__item-head-tags">
                  {{#each this.tags}}
                  <li class="header-menu__item-head-tag" style="background-color: {{this.bg}}; color: {{this.color}};">
                    {{this.text}}
                  </li>
                  {{/each}}
                </ul>
                {{/if}}
              </div>
              {{#if this.items}}
              <svg>
                <use xlink:href="#chevron">
              </svg>
              {{/if}}
            </a>
            {{#if this.items}}
            <div class="header-menu__item-body --level-3">
              {{#each this.items}}
              <div class="header-menu__item">
                <a class="header-menu__item-head" href="#">
                  <div class="header-menu__item-head-left">
                    <span>{{this.name}}</span>
                    {{#if this.tags}}
                    <ul class="header-menu__item-head-tags">
                      {{#each this.tags}}
                      <li class="header-menu__item-head-tag"
                        style="background-color: {{this.bg}}; color: {{this.color}};">
                        {{this.text}}
                      </li>
                      {{/each}}
                    </ul>
                    {{/if}}
                  </div>
                </a>
              </div>
              {{/each}}
            </div>
            {{/if}}
          </div>

          {{/each}}
        </div>
        {{/if}}
      </div>
      {{/each}}
    </nav>
    <div class="header-menu__burger-container">
      <button class="header-menu__burger" type="button">
        <span></span>
        <span></span>
      </button>
      <ul class="header-menu__burger-list">
        {{#each menu}}
        <li class="header-menu__burger-item js-burger-menu-item">
          <a class="header-menu__burger-item-link" href="">{{this.name}}</a>
        </li>
        {{/each}}
      </ul>
    </div>
  </div>
  <div class="header-menu__overlay"></div>
</div>